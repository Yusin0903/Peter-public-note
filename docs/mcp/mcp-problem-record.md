---
sidebar_position: 1
---

# MCP 使用問題記錄

## GitHub MCP 需要啟用 Docker

在使用 GitHub MCP server 時，需要先確認 Docker 有在運行。

如果你用的是 OrbStack（macOS 上的 Docker Desktop 替代品），使用前記得先開啟 OrbStack。

```bash
# 確認 Docker 是否在運行
docker ps

# 如果沒有回應，先開啟 OrbStack 或 Docker Desktop
```
